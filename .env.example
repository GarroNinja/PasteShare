# Environment Configuration
NODE_ENV=development

# Database connection (use the Transaction pooler URL for Vercel deployments)
DATABASE_URL=postgres://postgres.[project-id]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# Server configuration
PORT=3001

# Database sync flags (development only)
FORCE_SYNC=false
ALTER_SYNC=false
